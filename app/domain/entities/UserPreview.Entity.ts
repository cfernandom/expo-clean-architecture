import { EmailOV } from "../objectValues/email.ov";

export interface UserPreviewEntity {
    id: string;
    firstName: string;
    lastName: string;
    title: string;
    picture: string;
    //email: EmailOV;
}